# Workflow Model
Standard lifecycle: Draft → Submitted → Approved → Rejected → Returned → Closed.

Every action records actor, role, timestamp, remarks, prior state, new state, documents, SLA and escalation level.
