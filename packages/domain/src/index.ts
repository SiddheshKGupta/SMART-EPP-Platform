import {z} from "zod"; export const PurchaseTransactionSchema=z.object({leaseId:z.string().min(1),employerId:z.string().min(1),oemId:z.string().min(1),imei:z.string().min(8),invoiceValue:z.number().nonnegative(),baseValue:z.number().nonnegative(),invoiceDate:z.coerce.date(),schemeId:z.string().min(1),programmeId:z.string().min(1)}); export function assertUniqueImei(existing:string[],imei:string){if(existing.includes(imei))throw new Error("Duplicate IMEI")} export function assertWithinClaimTimeline(invoiceDate:Date,filingDate:Date,days:number){const elapsed=Math.floor((filingDate.getTime()-invoiceDate.getTime())/86400000);if(elapsed>days)throw new Error("Claim timeline expired")} export function assertMakerChecker(maker:string,checker:string){if(maker===checker)throw new Error("Maker cannot approve own work")} export function assertBatchMutable(status:string){if(["APPROVED","LOCKED"].includes(status))throw new Error("Approved batch is immutable")}
